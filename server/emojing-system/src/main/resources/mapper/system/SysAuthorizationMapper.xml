<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.china.soft.system.provider.dal.mapper.SysAuthorizationMapper">

    <select id="selectByName" resultType="com.china.soft.system.provider.dal.entity.SysAuthorization">
        SELECT * FROM sys_authorization a WHERE a.identifier = #{username}
    </select>
</mapper>