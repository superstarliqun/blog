<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.china.soft.system.provider.dal.mapper.ArticlesCategoryMapper">

    <select id="selectListGroup" resultType="com.china.soft.system.provider.dal.entity.ArticlesCategory">
        SELECT
            a.id,
            a.category_name,
            b.number
        FROM
            t_articles_category a
                LEFT JOIN (SELECT COUNT(id) AS `number`,category FROM t_articles WHERE category IS NOT NULL  GROUP BY category) b ON a.id = b.category
        WHERE
            a.is_deleted = 0
          AND a.pid = 0
    </select>
</mapper>